version: '3.8'

services:
  # PostgreSQL Datenbank
  postgres:
    image: postgres:16-alpine
    container_name: lspd-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: lspd_personalsystem
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend (optional, für lokale Entwicklung ohne Docker)
  # server:
  #   build:
  #     context: ./server
  #     dockerfile: Dockerfile
  #   container_name: lspd-server
  #   restart: unless-stopped
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:password@postgres:5432/lspd_personalsystem?schema=public
  #     - NODE_ENV=production
  #   env_file:
  #     - ./server/.env
  #   ports:
  #     - "3001:3001"

volumes:
  postgres_data:
